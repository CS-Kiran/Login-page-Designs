Check for file locations if not working properly.
Used PostgreSQL for backend connections.
To run the code properly :
  a. Make sure that apache server is turned on.
  b. Surf to location usinh URL : http://localhost/Video Background/login.html
  c. Check for your database connrctivity with pgAdmin4 in case of PostgreSQL
